import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    textBtn: {
        marginTop: 12
    },
    textInputBtn: {
        marginTop: 12
    },
    imageBtn: {
        marginTop: 12
    },
    loaderBtn: {
        marginTop: 12
    }
});

export default styles